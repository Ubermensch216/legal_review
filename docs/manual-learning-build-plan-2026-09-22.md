# 외부 전문가 질의 학습 루프 — 단계별 구축 계획

- 작성일: 2026-09-22
- 대상: `D:\Dev\legal_review` (HEAD `2c93355` + 미커밋 작업 트리)
- 근거 문서: `human_bridge_cure_review_report.md`(기획서), `docs/research/human-bridge-adoption-2026-09-22.md`(채택·반려 판단 근거)
- 코드상 명칭: **manual learning** (기획서의 "Human-Bridge CURE" / "Verified External Knowledge Loop")

---

## 1. 목표

소형(로컬) AI가 스스로 판단하지 못한 쟁점을 **비식별 질의서**로 만들고, 사용자가 대형 AI에서 받아온 답변을 **검증 가능한 지식 카드**로 바꿔 **같은 사건의 최종 검토에 되먹이는** 폐루프를 완성한다. 외부 AI API는 호출하지 않는다.

### 완성 후 사용자 흐름

```text
검토 실행
  → [탭 5] 외부 전문가 질의
     ① 로컬 AI가 미해결 질문 N개 추출 → 질의서 1건 생성
     ② 비식별 내역 확인 → 반출 준비 확인(READY)
     ③ 질의서 복사 → 대형 AI에 질의 → 답변 붙여넣기 (여러 번 가능)
        → 답변이 커버한 질문 자동 표시 (답변 3/4)
     ④ 지식 카드 검토 → 승인
     ⑤ 전 질문 충족 → [승인된 지식으로 최종 검토 다시 실행]
  → 최종 답변서 (사용한 지식·제외된 지식 사유 표시)
```

---

## 2. 핵심 설계 결정

| # | 결정 | 이유 |
|---|---|---|
| D1 | **질의서는 쟁점별로 쪼개지 않는다.** 질의서 1건 + 질문 N항목 | 쟁점마다 반출 승인을 반복시키면 형식적 승인이 된다. 조각내 여러 번 반출하면 조합 재식별 위험이 커진다. `preservedLogic`(유지해야 할 판단 조건)이 잘린다 |
| D2 | **답변은 여러 번 반입한다.** 답변 1건 → 카드 1장, 질문 다수 커버 | `importAnswer`가 이미 호출마다 카드를 만든다. 대형 AI는 질문 여러 개를 한 답변에 몰아 답한다 |
| D3 | **외부 답변 원문을 저장하지 않는다.** 구조화 카드 + 원문 해시만 | 원문에는 미검증 인용과 인젝션 문자열이 남는다. 개인정보 삭제 대상이 늘어난다. 기획서 §16 `RAW_EXTERNAL`은 반려 |
| D4 | **신규 테이블을 만들지 않는다.** `manual_learning` 단일 테이블 유지 | `kind`+`parent_id`+`revision`으로 기획서 §19의 두 테이블을 이미 표현한다. 늘리면 개인정보 삭제 경로가 갈라진다 |
| D5 | **원문 대비 diff를 보여주지 않는다.** 치환 토큰 하이라이트 + 건수 | 반출 화면에서 비식별 이전 원문을 다시 내려보내는 것은 기능 목적에 반한다 |
| D6 | **긴 답변은 컨텍스트를 키우기보다 쪼갠다** | 2B급 모델에 12k 토큰 한국어 법률문을 한 번에 물리는 것보다 2k씩 나눠 물리는 쪽이 누락이 적다. 호출 증가는 품질 대가를 지불하고 사는 것 |
| D7 | **구조화 JSON 직접 반입은 사용자가 명시적으로 고른다.** 자동 감지 금지 | 붙여넣은 내용이 시스템 동작을 바꾸면 그 자체가 인젝션 표면이 된다 |
| D8 | **지식이 제외된 사유는 반드시 화면에 표시한다** | 조용한 제외는 "답변을 다 넣었는데 반영이 안 됐다"로만 보인다. 기능이 아니라 신뢰성 표시다 |

### 이번 범위에서 제외하는 것

- 모델 재학습·파인튜닝 (기획서 §14.1 — 기획서도 비권장)
- 외부 AI API 자동 호출
- 5단계 Trust Level (현행 `DRAFT/READY/APPROVED/REVOKED` 유지)
- 벡터 DB·임베딩 검색 (SQLite 구조화 필터 + 키워드로 시작)
- 다중 사용자 감사 로그(누가 승인했는지) — 단일 사용자 전제

---

## 3. 현재 상태

**서버는 대부분 구현되어 있다**(미커밋). `manualLearning.js` / `manualLearningPrivacy.js` / `manualLearningMemory.js` / `manualLearningStore.js` / `manualLearningLocal.js` / `manualLearningApi.js` + `/api/law/learning/*`(`lawApi.js:21`) + 검토 프롬프트 주입(`lawWorkbenchReview.js:89`, `reviewContext.js:52`).

**없는 것: UI, 테스트, 그리고 아래 4개의 실동작 결함.**

| 결함 | 위치 | 증상 |
|---|---|---|
| ~~F1. 답변 길이 상한과 모델 예산이 15배 어긋남~~ **(정정 — 아래 참조)** | `manualLearning.js:100` vs `manualLearningLocal.js` | 라이브러리 기본 설정에서만 성립. 이 PC의 운영 설정에서는 해당 없음 |
| F2. 질문이 마크다운 본문에만 존재 | `manualLearning.js:70` | 질문별 진행 추적 불가 |
| F3. 사건 내 지식이 키워드 게이트에 걸림 | `manualLearningMemory.js:52` | 방금 만든 카드가 자기 사건 재검토에서 조용히 빠짐 |
| F4. 참고 지식 예산 2,000자 / 최대 2장 | `reviewContext.js:10`, `manualLearningMemory.js:52` | 질문 5개에 답을 다 받아도 2장만 반영 |

### F1 정정 — 운영 `.env`가 이미 예산을 올려두었다

최초 진단은 라이브러리 기본값(`contextTokens` 16,384 / 한글 1.9 토큰·자)으로 계산한 것이다.
그러나 이 PC의 `.env`에는 모델별 프로파일이 이미 설정되어 있다.

```text
LLM_MODEL_BUDGETS={"ollama:gemma4:e2b":{"contextTokens":40960,"outputTokens":8192,"tokensPerChar":{"hangul":0.75}}}
```

두 설정에서 실제로 넣을 수 있는 한국어 입력량(측정값):

| 설정 | 출력 예약 | `inputLimit` | 한국어 입력 상한 |
|---|---|---|---|
| 라이브러리 기본 | 8,192 | 7,680 | 약 3,400자 |
| 라이브러리 기본 + 작업별 예산(card) | 5,120 | 10,752 | 약 4,800자 |
| **이 PC 운영 설정** | 8,192 | 32,256 | **약 37,000자** |
| 이 PC 운영 설정 + 작업별 예산(card) | 5,120 | 35,328 | 약 40,600자 |

**즉 이 PC에서는 24,000자 답변이 이미 예산 안에 들어간다. "사실상 모든 답변이 413"이라는 진단은 이 배포에는 해당하지 않는다.**
기본 설정으로 배포되는 다른 PC에서는 여전히 성립하므로 Phase 1의 예산 분리는 유지한다.

**다만 이 설정에는 별도의 위험이 있다.** 프로파일의 `hangul: 0.75`는 추정 계수이고,
실제 토크나이저가 이보다 크면 추정은 통과하지만 Ollama가 `num_ctx`를 넘는 프롬프트의
앞부분을 **조용히 잘라낸다.** 잘린 입력으로도 JSON은 정상 출력되므로 파싱만으로는 알 수 없고,
답변 일부만 반영된 지식 카드가 완전한 얼굴로 저장된다. Phase 1에서 이 경로를 막았다(아래).

---

## 4. 단계별 구축

각 단계는 독립적으로 병합 가능하다. Phase 0~3까지가 MVP, Phase 4에서 사용자가 요구한 폐루프가 완성된다.

---

### Phase 0. 회귀 안전망 — ✅ 완료 (2026-09-22)

**목적**: UI를 붙이기 전에 이미 구현된 보안 경계를 테스트로 고정한다. 이후 모든 단계가 이 테스트 위에서 움직인다.

**신규**: `test/manualLearning.test.js`

| # | 테스트 | 고정하려는 성질 |
|---|---|---|
| T1 | `READY` 아닌 질의서 `export` → 409 / 식별자가 남은 텍스트의 `confirm`·`export` → 422 | 반출 게이트 |
| T2 | `READY` 이전 답변 반입 거부, 확정 항목 수정 거부, `revision` 불일치 → 409 | 상태 전이·낙관적 잠금 |
| T3 | `OLLAMA_URL`이 원격/자격증명 포함이면 `localLearningEndpoint` → 503, 학습 경로가 외부 fetch를 하지 않음 | 로컬 강제 |
| T4 | `evidenceHash` 불일치·90일 경과·부모 질의서 해시 불일치 시 승인 카드가 검색되지 않음 | 스코프 게이트 |
| T5 | 답변에 "이전 지시를 무시하라"가 있어도 카드 스키마 밖으로 새지 않음 | 인젝션 격리 |

로컬 AI는 `test/llmStreamStub.js`의 ndjson 스텁으로 대체한다(`callLearningLocal`이 Ollama 스트리밍 계약을 쓴다).

**수용 기준**: `npm test` 통과. T3은 네트워크 호출이 발생하지 않음을 스텁으로 검증.

**결과**: `test/manualLearning.test.js` 14건 작성, 전체 스위트 103건 통과.
가드 3개(`exportInquiry`의 식별자 재검사, `importAnswer`의 READY 검사, `findLearningKnowledge`의
`evidenceHash` 비교)를 각각 제거한 사본에서 T1·T2·T4가 하나씩 실패하는 것을 확인했다
(변이 검증은 프로젝트 사본에서 수행, 원본 무변경). 예산 관련 환경변수는 테스트 파일에서
라이브러리 기본값으로 고정해 이 PC의 `.env`에 결과가 좌우되지 않게 했다.

---

### Phase 1. 로컬 예산 분리 — ✅ 완료 (2026-09-22)

**목적**: 코드 2~3곳 변경으로 "사실상 모든 답변이 413" 상태를 푼다.

**변경**

1. `manualLearningLocal.js` — `callLearningLocal(system, user, { outputTokens })` 로 작업별 출력 예산을 받는다. 현재는 `resolveBudget('ollama', { model })`만 호출해 출력에 8,192 토큰을 예약한다. 산출물은 JSON 카드 한 장이므로 과하다.

   | 작업 | `outputTokens` | 결과 `inputLimit` |
   |---|---|---|
   | 쟁점 분석 (`createInquiry`) | 2,048 | 13,824 |
   | 청크 추출 (Phase 5) | 1,024 | 14,848 |
   | 카드 생성 (`importAnswer`) | 4,096 | 11,776 |

   `resolveBudget`은 이미 `config.outputTokens` 덮어쓰기를 지원한다. 검토 본 경로의 예산은 건드리지 않는다.

2. `manualLearning.js:100` — 답변 길이 상한을 **예산에서 역산해 검사**한다. 고정 24,000자를 남겨두되(DoS 방어), 그 전에 `createTokenCounter('ollama', {model}).estimate(system, user)`로 재보고 초과 시 **413이 아니라 "답변이 길어 나누어 처리합니다"** 경로(Phase 5)로 보낸다. Phase 5 이전에는 초과 문자수를 알려주는 400으로 돌린다: `"답변이 약 N자 초과입니다. 핵심 부분만 붙여넣거나 OLLAMA_NUM_CTX를 늘리십시오."`

3. `manualLearningLocal.js` — **모델이 보고한 실제 입력 토큰(`prompt_eval_count`)과 대조한다.**
   추정 계수가 실제보다 작으면 Ollama가 프롬프트 앞부분을 조용히 잘라내고, 잘린 입력으로도
   JSON은 정상 출력되어 파싱만으로는 알 수 없다. 실제 토큰이 한도를 넘으면 결과를 버리고
   413으로 알리며, 관측값을 `recordObservation`에 넣어 다음 추정을 보정한다.

4. `manualLearningLocal.js` — **출력이 잘린 경우와 응답이 깨진 경우의 오류를 분리한다.**
   전자는 `OLLAMA_NUM_PREDICT`를, 후자는 Ollama 상태를 봐야 하므로 조치가 다르다.
   메시지에는 토큰 수만 넣고 모델 원문은 올리지 않는다.

**수용 기준**: 예산 초과 시 **얼마나 초과했는지**가 오류에 담긴다. 실제 입력이 예산을 넘으면 결과를 저장하지 않는다.

**결과**: 작업별 출력 예산으로 `inputLimit`이 7,680 → 10,752(카드) / 12,800(분석) 토큰으로 늘었고,
같은 시스템 프롬프트 기준 한국어 입력 상한이 약 3,400자 → 4,800자 / 5,700자가 되었다.
운영자가 `OLLAMA_NUM_PREDICT`를 더 작게 잡은 경우에는 그 설정을 넘지 않는다.
`createInquiry`의 45,000자 고정 상한도 예산 역산 검사로 교체했다(같은 결함이 분석 호출에도 있었다).

**테스트**: T3(실제 토큰 초과 시 폐기, 잘림/깨짐 오류 구분), T6(예산 초과 안내, 작업별 예산 속성) 4건 추가.

---

### Phase 2. 질문 항목화와 커버리지 — F2 해결

**목적**: "답변 3/4" 를 가능하게 하는 데이터 모델 변경. 백엔드 전용.

**변경**

1. `manualLearning.js:49` `createInquiry` — 로컬 AI가 낸 `questions`를 마크다운 본문에만 넣지 말고 `store.create`의 데이터에 `questions: [{ id, text }]`로 **함께 저장**한다. 본문 렌더링은 이 배열에서 생성해 번호를 일치시킨다.

2. `manualLearning.js:98` `importAnswer` — 로컬 AI 출력 스키마에 `answeredQuestions: [질문 번호]`를 추가한다. 카드 데이터에 `answeredQuestions`, `answerHash`(원문 해시, D3), `providerLabel`(사용자 입력 자유문자열, 신뢰도에 미반영)을 저장한다.

3. 조회 응답에 **커버리지 요약**을 포함한다: 질문별 `{ 미답변 | 답변됨(카드 id) | 승인됨 }`.

**주의**: 추출 단계 프롬프트에는 **질의서 전문이 아니라 번호 붙은 질문 목록만** 넣는다. 전문을 함께 보내면 Phase 5에서 청크마다 그 비용을 반복 지불하게 된다.

**수용 기준**: 답변 1건을 반입하면 그 답변이 커버한 질문만 상태가 바뀐다. 두 번째 답변 반입 시 나머지가 채워진다.

**테스트**: T7 — 답변 2건으로 질문 4개가 모두 충족되면 커버리지가 4/4가 된다. 범위 밖 번호를 내면 무시한다.

---

### Phase 3. 탭 UI

**목적**: 서버에 이미 있는 기능을 사람이 쓸 수 있게 만든다.

**변경**: `public/index.html`(탭 추가, 기존 4탭은 `:180`), `public/js/learningTab.js`(신규), `public/css/workbench.css`. 탭 전환 로직은 `public/js/lawWorkbench.js:7`을 그대로 쓴다.

```text
[5] 외부 전문가 질의   [배지: 답변 2/4]

── STEP 1. 미해결 쟁점 ───────────────────────
 로컬 AI가 식별한 미해결 질문 4건      [질의서 생성]
 (needsHelp=false → "추가 질의 필요 쟁점 없음" + 쟁점 직접 입력란)

── STEP 2. 외부 반출 전 검토 ──────────────────
 치환 내역: 성명 2 · 주소 1 · 전화 1 · 비공개어 3
 [질의서 본문 — 편집 가능, 치환 토큰 하이라이트]
 추가로 가릴 단어: [____] +
 ☐ 개인정보·비밀정보 제거 확인   ☐ 판단 조건 보존 확인
                                   [반출 준비 확인] → READY

── STEP 3. 답변 접수 ─────────────────────────
 Q1 …  ● 답변됨 → 카드 A (승인)
 Q2 …  ● 답변됨 → 카드 A (검토 대기)
 Q3 …  ○ 미답변
 Q4 …  ○ 미답변
 [질의서 복사]   [답변 붙여넣기 ▾ (전체 답변 / 구조화 JSON)]

── STEP 4. 지식 카드 ─────────────────────────
 카드 A  인용검증 3/4 (제12조 미확인)
 적용조건 / 예외 / 검토원리 / 점검순서   [편집] [승인] [폐기]

── STEP 5. 최종 답변서 ───────────────────────
 [승인된 지식으로 최종 검토 다시 실행]   ← Phase 4
```

**구현 시 반드시 지킬 것**

- **복사 버튼은 `GET /api/law/learning/inquiries/:id/export`를 반드시 경유한다.** 이 엔드포인트가 복사 직전에 `assertNoDetectedIdentifiers`로 식별자를 재검사한다. 편의상 `item.text`를 클립보드에 직접 넣으면 서버 측 최종 안전장치가 통째로 무력화된다.
- `revision` 충돌(409)은 "내용이 변경되었습니다. 다시 불러오십시오"로 그대로 노출한다. 조용한 덮어쓰기는 승인 근거를 흔든다.
- 이 탭은 `learningMode === 'manual'`(로컬 Ollama)에서만 의미가 있다. 클라우드 제공자 선택 시 비활성화하고 사유를 표시한다(`lawWorkbenchReview.js:91`이 ollama에서만 지식을 주입한다).
- 카드의 `legalValidity: 'NOT_CERTIFIED'`와 `sourceLabel`을 화면에서 숨기지 않는다.

**수용 기준**: 검토 1건에 대해 질의서 생성 → 반출 확인 → 복사 → 답변 붙여넣기 → 카드 승인까지 UI만으로 완주 가능.

---

### Phase 4. 폐루프 — F3·F4 해결 (사용자 요구의 핵심)

**목적**: 승인된 지식이 **같은 사건의 최종 검토**에 반영되게 한다.

**변경**

1. `manualLearningMemory.js:39` `findLearningKnowledge(context, query, store, { historyId })` — **해당 사건에서 파생된 승인 카드는 키워드 2개 매칭을 건너뛰고 항상 후보에 넣는다.** 승인 상태 · 부모 질의서 해시 일치 · `evidenceHash` 일치 · 인용 전건 `VERIFIED_EXISTENCE` 네 조건은 **그대로 유지한다**(이 넷이 실제 안전장치다). `slice(0, 2)`는 사건 내 경로에서 6장까지 허용한다.

2. 같은 함수가 **제외된 카드와 사유**를 함께 반환한다: `{ used: [...], excluded: [{ id, title, reason }] }`. 사유는 `EVIDENCE_CHANGED` / `EXPIRED` / `CITATION_UNVERIFIED` / `PARENT_MODIFIED` / `BUDGET` 중 하나.

3. `reviewContext.js:52` — `buildReviewInput`은 이미 4번째 인자로 `budgets` 덮어쓰기를 받는다(`reviewContext.js:37`). 사건 내 재검토 경로에서만 `{ learningKnowledge: 6000 }`을 넘긴다. **전역 `DEFAULT_SECTION_BUDGETS`는 건드리지 않는다** — 올리면 공식 조문·판례 발췌가 밀려난다. 예산 때문에 빠진 카드는 `BUDGET` 사유로 집계한다.

4. `lawWorkbenchReview.js:89` — 제외 사유를 `warnings`에 문장으로 싣는다. 예: `"기준 근거가 변경되어 학습 지식 2건을 적용하지 않았습니다."`

5. `lawApi.js:74` / `lawHistoryDb.js:52` — 재검토 결과 저장 시 `reviewData.meta`에 `sourceHistoryId`, `usedInquiryIds`, `usedKnowledgeIds`를 남긴다. 기획서 §18의 Learning Episode 추적 목적은 이것으로 충족된다. **별도 테이블은 만들지 않는다**(D4).

6. UI STEP 5 — 전 질문 충족 시 버튼 활성화. **부분 충족도 실행은 허용하되** 미충족 질문을 최종 답변서의 제한사항에 명시한다. 차단하면 사용자가 아무 답변이나 붙여넣어 게이트를 통과시키려 한다.

**수용 기준**: 답변을 모두 반입·승인한 뒤 최종 검토를 실행하면 승인 카드가 프롬프트에 실리고, 결과 화면에 사용한 지식 목록과 제외된 지식의 사유가 함께 보인다.

**테스트**: T8 — 같은 사건의 승인 카드가 키워드 매칭 없이도 주입된다. T9 — `evidenceHash`가 바뀌면 주입되지 않고 사유가 `EVIDENCE_CHANGED`로 반환된다. T10 — 사건 내 예산 상향이 다른 섹션 예산을 침범하지 않는다.

---

### Phase 5. 긴 답변 처리 — D6·D7

**목적**: Phase 1로도 넘치는 답변(1만 자 이상)을 처리한다.

#### 5-A. 구조화 JSON 직접 반입 (먼저, 싸다)

질의서는 이미 대형 AI에게 마지막에 카드 JSON으로 정리해 달라고 요청한다(`CARD_SCHEMA`). 대형 AI가 응하면 붙여넣을 것은 JSON 블록 하나이고 **로컬 AI 호출이 0회**다. 경로는 `validateKnowledgeCard` → `redactLearningText` → `checkLearningCitations`.

**사용자가 UI에서 명시적으로 고르는 경로여야 한다**(D7). 붙여넣은 내용에 JSON이 보인다고 자동으로 빠른 경로를 타면, 외부 답변의 내용이 시스템 동작을 바꾸는 셈이다.

#### 5-B. map-reduce 청킹

```text
비식별(전체 1회) → 청킹 → [추출 × N] → 통합 → 카드
```

| 규칙 | 내용 |
|---|---|
| R1 | **비식별은 청킹 전에 전체 텍스트에 한 번만.** `redactLearningText`는 호출 단위로 `[성명_1]`, `[성명_2]` 번호를 매긴다. 청크별로 돌리면 같은 사람이 청크마다 다른 번호가 되어 동일인 판단이 깨진다 |
| R2 | **청크 크기는 상수로 박지 않는다.** `createTokenCounter(...).estimate()`로 `inputLimit`의 70% 선까지 실측으로 채운다. "3,000자"로 박으면 `OLLAMA_NUM_CTX`나 모델 변경 시 깨진다 |
| R3 | **주장과 근거가 갈라지는 경계에서 자르지 않는다.** 제목·빈 줄 경계로 자르고 1~2문장 겹친다. 한국어 법률 답변은 "…할 수 있다. 근거: 공유재산법 제20조" 형태라 고정 글자수로 자르면 근거만 떨어진 청크가 생긴다 |
| R4 | **주장 없는 인용은 버린다.** 근거만 있는 청크의 인용을 그대로 올리면, 존재 검증만 통과해 "검증됨"처럼 보인다. 누락보다 나쁘다 |
| R5 | **추출 단계에 질의서 전문을 넣지 않는다.** 번호 붙은 질문 목록만 넣는다(Phase 2). 통합 단계에서만 전문을 쓴다 |
| R6 | **통합 단계는 병합·중복제거만 한다.** 새 주장 추가 금지. 인용은 청크에서 온 문자열 그대로 통과시키고, 이후 `checkLearningCitations`로 다시 검증한다 |
| R7 | **부분 실패를 완성된 카드로 내지 않는다.** 카드에 `chunkCoverage: {total, processed}`를 남기고, 미완이면 `approveKnowledge`를 막는다. 답변의 40%로 만들어진 지식이 완전한 얼굴로 저장되어 이후 검토에 재사용되는 것이 이 설계의 최악 실패 모드다 |
| R8 | **시간 예산.** `callLearningLocal`의 타임아웃은 호출당 180초다. 12청크면 최악 36분 동안 HTTP 요청이 물린다. 청크 수 상한(12) + 전체 마감시간 + 진행 표시(SSE) 또는 비동기 잡 중 하나는 함께 간다 |

**수용 기준**: 15,000자 답변이 카드 1장으로 정리되고, 인용이 주장과 함께 보존되며, 중간 실패 시 카드가 미완으로 표시되어 승인이 막힌다.

**테스트**: T11 — 청크 경계에 걸친 "주장+근거"가 보존된다. T12 — 청크 3개 중 2번이 실패하면 카드가 `chunkCoverage` 미완으로 표시되고 승인이 409로 막힌다. T13 — 비식별 토큰 번호가 청크 전체에서 일관된다.

---

### Phase 6. 검증 보강 (후속)

기획서 §13의 잔여분. 급하지 않다.

- `checkLearningCitations` 확장: 판례번호·유권해석 번호 존재 검증(`decisionsApiClient` 연계)
- 답변 주장과 공식 근거의 **모순 표시**(현재는 존재 여부만 검증)
- 카드별 사용 횟수·실패 이력 집계 → 재사용 우선순위

---

## 5. 데이터 모델 변경 요약

`manual_learning` 테이블 스키마는 그대로 두고 `data` JSON에만 필드를 추가한다. 마이그레이션 불필요.

| 대상 | 추가 필드 | 단계 |
|---|---|---|
| inquiry | `questions: [{ id, text }]` | Phase 2 |
| knowledge | `answeredQuestions: [번호]` | Phase 2 |
| knowledge | `answerHash`, `providerLabel` | Phase 2 |
| knowledge | `chunkCoverage: { total, processed }` | Phase 5 |
| 검토 이력 `reviewData.meta` | `sourceHistoryId`, `usedInquiryIds`, `usedKnowledgeIds` | Phase 4 |

---

## 6. 위험과 대응

| 위험 | 대응 | 단계 |
|---|---|---|
| 비식별 실패 (조합 재식별) | 질의서를 쪼개지 않음(D1) + 패턴 치환 + 사용자 지정어 + 반출 시 재검사 + 사람 승인 | 기존 |
| 반출 게이트 우회 | 복사는 반드시 `export` 엔드포인트 경유, 클라이언트 직접 복사 금지 | Phase 3 |
| 외부 AI 환각 | 인용 존재·시행일 검증, 미검증 인용이 하나라도 있으면 재사용 검색에서 제외 | 기존 / Phase 6 |
| 프롬프트 인젝션 | 답변을 데이터로만 처리, JSON 스키마 강제, 자동 경로 전환 금지(D7) | 기존 / Phase 5 |
| 법령 개정 후 과거 지식 재사용 | `evidenceHash` 일치 + 90일 실효 | 기존 |
| **부분 처리된 지식이 완전한 것처럼 저장** | `chunkCoverage` 미완 시 승인 차단 | Phase 5 |
| **지식이 조용히 빠져 사용자가 모름** | 제외 사유 반환·표시 | Phase 4 |
| 긴 처리 시간에 대한 무피드백 | 청크 상한·마감시간·진행 표시 | Phase 5 |

---

## 7. 완료 정의

- [ ] Phase 0 테스트 5건 + 각 단계 테스트(T6~T13) 통과, `npm test` 녹색
- [ ] 6,000자 답변이 단일 호출로, 15,000자 답변이 청킹으로 카드가 된다
- [ ] 질문별 답변 상태가 탭 배지에 보인다
- [ ] 질의서 복사가 `export` 엔드포인트를 경유한다(클라이언트 직접 복사 경로 없음)
- [ ] 승인된 지식이 같은 사건의 최종 검토 프롬프트에 실린다
- [ ] 제외된 지식의 사유가 결과 화면에 보인다
- [ ] 최종 검토 이력에 원 사건 id와 사용 지식 id가 남는다
- [ ] 미완 카드는 승인할 수 없다
- [ ] 클라우드 제공자 선택 시 탭이 비활성화되고 사유가 보인다

---

## 8. 남은 판단 사항

- **90일 실효 기간**: 사건 내 루프에는 무관하나 장기 보관 지식의 기본값으로 적절한지는 운영 판단.
- **다중 사용자**: 현재 승인 시각은 남지만 "누가"는 남지 않는다. 기관 배포 전에 정해야 한다.
- **`OLLAMA_NUM_CTX` 상향 여부**: Phase 1의 예산 분리로 부족하면 검토. 다만 소형 모델의 장문 이해도 문제는 컨텍스트 확대로 해결되지 않는다(D6).
